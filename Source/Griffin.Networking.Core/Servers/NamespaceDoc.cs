using System.Runtime.CompilerServices;

namespace Griffin.Networking.Servers
{
    /// <summary>
    /// Server implementation. 
    /// </summary>
    /// <remarks>All servers are divided into two parts. The core which will accept all clients and monitor their state (all classes derived from <see cref="ServerBase"/>) and the
    /// actual class which takes care of each client (<see cref="IServerService"/>).</remarks>
    [CompilerGenerated]
    internal class NamespaceDoc
    {
    }
}